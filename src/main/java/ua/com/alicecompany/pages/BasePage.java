package ua.com.alicecompany.pages;

public class BasePage {
    /*public static final Duration DATA_TIMEOUT_SEC = Duration.ofSeconds(60);

    WebDriver driver;

    public BasePage() {
        driver = DriverConfiguration.getDriver();
        PageFactory.initElements(driver, this);
    }

    public BasePage(WebDriver driver) {
        PageFactory.initElements(driver, this);
        this.driver = driver;
    }

    public WebDriver getWebDriver() {
        return driver;
    }*/
}
